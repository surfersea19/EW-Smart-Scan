import { PredictedActivityPanel } from "./PredictedActivityPanel";

// Preserved for backwards compatibility with any older references
export function EmitterPanel() {
  return <PredictedActivityPanel />;
}
